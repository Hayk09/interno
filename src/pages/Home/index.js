import React from "react";
import { Flex} from "@chakra-ui/react";
// import { useGetUserByNameQuery, useGetUsersByNameQuery } from "../../services/loginApi";


const Home = () => {
    // const {data:users} = useGetUsersByNameQuery()
    // const {data:todos} = useGetUserByNameQuery(1)
    // console.log(users,todos)
    return(
        <Flex>Home</Flex>
    )
}
export default Home